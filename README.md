# Sky cargo

Локальная среда разработки: **OpenServer 6** + **PHP 8.4** + **MySQL 8.4**.

## 🛠 Особенности проекта
- В Git хранится только **тема** и необходимые конфигурационные шаблоны.

## 📂 Структура репозитория
- `wp-content/themes/sky-cargo/` — папка с разрабатываемой темой.
- `.gitignore` — настроен на игнорирование ядра WP и системных файлов OpenServer.

## 🚀 Как развернуть проект локально

1. **Подготовка WordPress:**
   Установите чистую версию WordPress в папку вашего домена в OpenServer.

2. **Клонирование темы:**
   Склонируйте этот репозиторий в корень вашего сайта. Ваша тема должна оказаться в `wp-content/themes/`.

3. **Настройка базы данных:**
   - Создайте новую БД в phpMyAdmin.
   - Впишите данные вашей БД в `wp-config.php`.

4. **Настройка инструментов сборки:**
   - cd wp-content/themes/sky-cargo/
   - npm install

5. **Активация:**
   Зайдите в админ-панель WordPress (`/wp-admin`) и активируйте тему **"Sky Cargo"** в разделе "Внешний вид".

6. **Процесс разработки**
   - Чтобы Vite отслеживал изменения в src/ и обновлял dist/ на лету, запустите:
   - npm run watch 