# Sky cargo

Локальная среда разработки: **OpenServer 6** + **PHP 8.4** + **MySQL 8.4**.

## 🛠 Установка проекта локально
1. Склонируйте репозиторий в папку `OSPanel/home/*my-site*`.
2. Создайте базу данных в phpMyAdmin (например, `wp_db`).
3. Скопируйте `wp-config-sample.php` в `wp-config.php` и укажите данные БД:
   - Хост: `127.0.0.1`
   - Пользователь: `root`
   - Пароль: `` (пусто)
4. Запустите модули в консоли OSP:
   ```bash
   osp on PHP-8.4
   osp on MySQL-8.4